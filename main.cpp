#include "Real_CPU.h"
#include <climits>
#include <iostream>
#include <fstream>
#include <string>

#include "Def_types.h"

int main() {
	std::string inputfile = "input.in";
	Sfml_CPU cpu(inputfile);
	return 0;
}